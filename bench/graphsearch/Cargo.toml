[package]
name = "bfs-graph-search"
default-run = "bfs-graph-search"
version = "0.0.1"
authors = [ "Yash Kumar <tyushk+git@gmail.com>" ]
edition = "2021"

[profile.dev]
opt-level = 3

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

[[bin]]
name = "bfs-graph-search"
path = "src/main.rs"

[dependencies]
openshmem-rs = { path = "../../" }
openshmem-vec = { path = "../../crates/openshmem-vec" }
openshmem-matrix = { path = "../../crates/openshmem-matrix", features = [] }
rayon = "1.10.0"
rand = "0.9.0"
indicatif = { version = "0.17.11", features = ["rayon"] }
rustc-hash = "2.1.1"
