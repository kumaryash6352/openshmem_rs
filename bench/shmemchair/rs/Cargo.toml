[package]
name = "shmembench-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5.22", features = ["derive"] }
# update revision as wanted
openshmem-rs = { path = "../../../" }
