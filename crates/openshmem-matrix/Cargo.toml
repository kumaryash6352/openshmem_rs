[package]
name = "openshmem-matrix"
version = "0.1.0"
edition = "2024"

[dependencies]
openshmem-rs = { path = "../../" }
openshmem-vec = { path = "../openshmem-vec/" }
rayon = "1.10"

[features]
debug = []
